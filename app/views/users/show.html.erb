<% if current_user != set_user %>
<strong>We Miss You! <%= @user.username %> </strong>
<% else %>
<strong>Welcome Back, <%=@user.username %>!</strong>
<% end %>




<br>
<% if current_user != set_user %>
<%= link_to "login", login_path %>
   <% else %>
   
    <br>
    <%= link_to "My Reviews", user_reviews_path(@user.id) %>
    <br>
    <%= link_to "Leave A Review", new_user_review_path(@user.id) %>
    <br>
    <%= link_to "Add A Movie", new_user_movie_path(@user.id) %>
    <br>
    <br>
     <%= link_to "Logout", sessions_path, method: :delete %>
<% end %>